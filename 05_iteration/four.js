const myObject = {
    js: 'javascript',
    cpp: "C++",
    rb: 'ruby',
    swift: "swift by apple"
}
for (const key in myObject) {
 //  console.log(`${key} is shortform of ${myObject[key]} `);
   
}

const programming = ["html", "css", "javascript", "python"]

for (const key in programming) {
   console.log(programming[key]);
}